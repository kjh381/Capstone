@model SARDT.Models.PublicVM

    @{
        ViewBag.Title = "Home";
    }

    @Html.ActionLink("Im just here for testing purposes", "EditText", "WebContent")

    <!-- Jumbotron -->
    <div class="jumbotron">
        <p>
            @if (Model.currentVideo.Title != "invalid" && Model.currentVideo.URL != "invalid")
            {
                <iframe width="420" height="315" src="@Model.currentVideo.URL"></iframe>
            }
        </p>
    </div>

    <div class="row">
        <div class="col-sm-8">
            <h2>@Html.DisplayFor(model => model.textList[0].Section)</h2>
                @Html.DisplayFor(model => model.textList[0].Body)
        </div>
        <div class="col-sm-4">
            <figure>
                <img src="~/Images/@Model.imageList[0].FileName" width="300" height="300" />
                <figcaption>@Model.imageList[0].Caption</figcaption>
            </figure>
        </div>
    </div>

<div class="row">
    <div class="col-sm-12">
        <h3>Placeholder for Events listing</h3>
        <br /><br /><br />
    </div>
</div>

    <div class="row">
        <div class="col-sm-4">
            <figure>
                <img src="~/Images/@Model.imageList[1].FileName" width="300" height="300" />
                <figcaption>@Model.imageList[1].Caption</figcaption>
            </figure>
        </div>
        <div class="col-sm-8">
            <h2>@Html.DisplayFor(model => model.textList[1].Section)</h2>
            @Html.DisplayFor(model => model.textList[1].Body)
        </div>
    </div>