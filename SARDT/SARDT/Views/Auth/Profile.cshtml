@model SARDT.Models.Member

@{
    ViewBag.Title = "Profile";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Profile</h2>

<div>
    <h4>Event</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayName("Name")
        </dt>

        <dd>
            @if (Model.Name == null || Model.Name == "")
            {
                @Html.Display("Not Set", new { @class = "label label-warning"})
            }
            else
            {
                @Html.DisplayFor(model => model.Name)
            }
        </dd>

        <dt>
            @Html.DisplayName("User Name")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Email)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>


        <dt>
            @Html.DisplayNameFor(model => model.Address)
        </dt>

        <dd>
            @if (Model.Address == null || Model.Address == "")
            {
                @Html.Label("Not Set", new { @class = "label label-warning" })
            }
            else
            {
                @Html.DisplayFor(model => model.Address)
            }        
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.City)
        </dt>


        <dd>
            @if (Model.City == null || Model.City == "")
            {
                @Html.Label("Not Set", new { @class = "label label-warning" })
            }
            else
            {
                @Html.DisplayFor(model => model.City)
            }
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ZipCode)
        </dt>


        <dd>
            @if (Model.ZipCode == null || Model.ZipCode == 0)
            {
                @Html.Label("Not Set", new { @class = "label label-warning" })
            }
            else
            {
                @Html.DisplayFor(model => model.ZipCode)
            }
        </dd>

        <dt>            
            @Html.DisplayName("Phone Number")
        </dt>


        <dd>
            @if (Model.PhoneNumber == null || Model.PhoneNumber == "")
            {
                @Html.Label("Not Set", new { @class = "label label-warning" })
            }
            else
            {
                @Html.DisplayFor(model => model.PhoneNumber)
            }
        </dd>

        <dt>            
            @Html.DisplayName("Name of Emergency Contact")
        </dt>


        <dd>
            @if (Model.EmergencyContactName == null || Model.EmergencyContactName == "")
            {
                @Html.Label("Not Set", new { @class = "label label-warning" })
            }
            else
            {
                @Html.DisplayFor(model => model.EmergencyContactName)
            }
        </dd>

        <dt>
            @Html.DisplayName("Phone # of Emergency Contact")
        </dt>


        <dd>
            @if (Model.EmergencyContactPhone == null || Model.EmergencyContactPhone == "")
            {
                @Html.Label("Not Set", new { @class = "label label-warning" })
            }
            else
            {
                @Html.DisplayFor(model => model.EmergencyContactPhone)
            }
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit Profile", "EditProfile")
</p>
